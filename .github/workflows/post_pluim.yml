name: Post KNMI Afbeelding naar Discord

on:
  workflow_dispatch:
  schedule:
    - cron: "15 8 * * *"   # 09:15 NL-tijd (zomertijd)
    - cron: "15 20 * * *"  # 21:15 NL-tijd (zomertijd)

jobs:
  send-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pytz

      - name: Run script
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python pluim_to_discord.py
